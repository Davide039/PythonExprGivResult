from createAddition import create_addition
from createSubtraction import create_subtraction
from createMultiplication import create_multiplication
from createDivision import create_division
from createPow import create_pow
from createSqrt import create_square_root

n = int(input('Type a number: '))

create_addition(n)
create_subtraction(n)
create_multiplication(n)
create_division(n)
create_pow(n)
create_square_root(n)
